# Application Name
spring.application.name=Fitness Spring Web Application

# Enable H2 Database Console (for in-memory database access)
spring.h2.console.enabled=true

# Database Configuration (Replace with your actual database details if not using H2)
# Uncomment and configure the following properties for a real database connection
#spring.datasource.url=jdbc:mysql://localhost:3306/your_database_name
#spring.datasource.username=your_username
#spring.datasource.password=your_password

# Server Port (default: 8080)
#server.port=8080